/* Adding 3 Heights(integers)*/
#include<stdio.h>
void main ()
{
	double reddy = 6.1, sai = 5.7,mani = 5.7, sum = reddy+sai+mani;
	printf(" sum Height of those 3 guys are %lf, %lf,%lf= %lf",reddy,sai,mani,sum);
}
